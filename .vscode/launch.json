// Press F5 to open a new VS Code window with the themes loaded.
// Then pick a variant with Ctrl+K Ctrl+T.
{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Run Extension",
			"type": "extensionHost",
			"request": "launch",
			"args": ["--extensionDevelopmentPath=${workspaceFolder}"]
		}
	]
}
